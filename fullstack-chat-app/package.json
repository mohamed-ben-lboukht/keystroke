{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "Full Stack Chat App with Challenge Game",
  "main": "index.js",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\""
  },
  "keywords": ["chat", "game", "react", "node", "socket"],
  "author": "ENSICAEN",
  "license": "ENSICAEN",
  "dependencies": {
    "framer-motion": "^12.5.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
